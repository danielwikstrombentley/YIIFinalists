// Placeholder export. The real semantic-action type union, priority table (FR-019), dedup-key
// derivation, and v1 wire envelope (Zod) are implemented in T008. Contract tests land first in
// T006 (red-first) and must fail against this stub.
export const SEMANTIC_ACTIONS_PACKAGE_VERSION = '0.0.0';
