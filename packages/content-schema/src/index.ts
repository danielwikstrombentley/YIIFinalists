// Placeholder export. The real Zod schemas (Release/Category/Project/GeographicFraming/
// ContentOption/ContentSequence/Beat/MediaAsset/VoiceoverAsset/manifest/channels/editorial
// records) and the JSON Schema export script are implemented in T009. Contract tests land first
// in T007 (red-first) and must fail against this stub.
export const CONTENT_SCHEMA_PACKAGE_VERSION = '0.0.0';
