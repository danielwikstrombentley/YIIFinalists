> **Editorial note (added after this run, same date)**: this was a **dry run** requested by T005
> to validate `.github/agents/code-review.agent.md` end-to-end before relying on it as the
> mandatory phase-PR gate — not a real gating review (no PR existed yet). It correctly exercised
> the provider-independence check (see the companion same-provider refusal dry run, not saved to
> disk) and caught genuine registry-hygiene gaps (findings #2, #3), which were fixed immediately
> after. Findings #1 and #4, and the "merge-blocking CI" constitution-check row, are about the
> GitHub Actions workflow this run reviewed; that workflow was removed immediately afterward on
> explicit user instruction (no hosted CI in this project — see `CONTRIBUTING.md` "No hosted CI").
> They are superseded, not fixed-in-place, and are left below verbatim for the historical record.
> The real mandatory phase-level review runs again against the final PH1 diff before the phase PR
> merges to `main`.

# Code Review — phase/001-ph1-setup → main

**Reviewer**: GPT-5.6 Sol (OpenAI) · **Implementer(s)**: Claude Sonnet 5 (Anthropic)
**Provider independence**: PASS
**Scope**: T001, T002, T003, T004 · **Files reviewed**: 34 · **Date**: 2026-08-04

## Verdict: REQUEST CHANGES

The scaffold itself is sound: frozen install, typecheck, lint, format check, unit smoke test, production build, CLI-help smoke test, Playwright smoke test, diff hygiene, and dependency audit all pass locally; the build emits 390 Cesium files locally and no application CDN reference was found. Merge cannot yet be approved because T004’s required merge-blocking CI gate is not active or evidenced, T001–T004 use the `[R]` status despite there being no PR, and the diff also advances out-of-scope T005 to `[R]` while its work and dry-run remain incomplete.

## Findings

| # | Severity | File:Line | Issue | Suggested fix |
|---|----------|-----------|-------|---------------|
| 1 | MAJOR | [T004 requirements](specs/001-yii-led-experience/tasks.md#L168-L174), [CI trigger](.github/workflows/ci.yml#L4) | T004 requires CI failures to block merge and a green trivial-PR run. GitHub reports `main` as unprotected, no repository rulesets exist, and no PR/CI run exists yet. The workflow can run, but a failed result currently does not prevent merging; its Node 22 execution is also not yet evidenced. | Run both jobs on a PR, then configure and document enforced rules for `main` and `phase/**` requiring pull requests and the exact `Install, typecheck, lint, format, unit tests, build` and `Playwright smoke` checks. |
| 2 | MAJOR | [T001–T004 registry entries](specs/001-yii-led-experience/tasks.md#L144-L169) | T001–T004 are marked `[R]`, whose defined meaning is “PR open, awaiting review,” but every PR field is blank and no PR exists. This is stale registry state even though Owner/Branch provenance is now correct. | Keep these tasks `[~]` until a PR is open; then set `[R]` and record the PR number/URL consistently. |
| 3 | MAJOR | [T005 registry entry](specs/001-yii-led-experience/tasks.md#L176-L181) | The diff advances T005 to `[R]` even though T005 is outside this review’s scope, its dry-run is still being performed, no PR exists, and its declared contribution artifacts are absent. This is an unexplained out-of-scope completion claim. | Set T005 to `[~]` while it is being implemented; complete its declared artifacts and both provider-gate dry runs before moving it to `[R]` with a recorded PR. |
| 4 | MINOR | [verify checkout](.github/workflows/ci.yml#L17), [e2e checkout](.github/workflows/ci.yml#L49), [LFS rules](.gitattributes#L4-L19) | Both CI jobs use the checkout action’s default LFS behavior, so media covered by the new LFS rules will be left as pointer files. Once real assets land, build/E2E can validate pointers instead of the event assets. | Enable `lfs: true` on both checkout steps, or perform an explicit LFS pull before checks and builds. |
| 5 | MINOR | [audio LFS patterns](.gitattributes#L13-L15) | The planned delivery formats include AAC/Opus, but the media rules cover MP3/WAV/M4A only; raw AAC/Opus assets would enter normal Git. | Add LFS patterns for the approved AAC/Opus file extensions before voiceover assets land. |
| 6 | NIT | [endurance placeholder](apps/experience/package.json#L16) | The placeholder says the endurance soak is T067, but the registry assigns performance to T067 and endurance to T068. | Change the placeholder reference to T068. |

## Constitution check

| Principle/Gate | Status | Note |
|---|---|---|
| I — Deterministic state ownership | N/A | No state machine, navigation state, async state work, or cancellation path is introduced in this scaffolding slice. |
| II — Motion and sequence orchestration | N/A | No motion, timeline, media sequence, camera writer, or renderer handover is introduced. |
| III — Protocol-independent input | N/A | No transport or semantic-input implementation is introduced. |
| IV — Local-first reliability | PASS | The static build emits Cesium workers/assets locally, no application CDN reference was found, and no live AI/TTS or awaited logging path exists. |
| V — Reusable content architecture | PASS | Runtime, pipeline, shared contracts, and kiosk concerns are separated into workspace packages; no project-specific application code exists. Content-limit enforcement belongs to later schema/runtime tasks. |
| VI — Public presentation purity | PASS | The scaffold renders only a blank full-screen stage and exposes no menu, instructions, diagnostics, errors, or controls in the public DOM. |
| VII — Human approval and traceability | N/A | No drafting, editorial, validation, publishing, or release path is introduced. |
| VIII — Resource ownership and performance | N/A | No runtime renderer/resource, listener, object URL, ticker, RAF loop, or per-frame React state is acquired. |
| IX — Verification, observability, security | PASS | No credentials or non-local runtime URLs were found; strict TypeScript, lint, unit, E2E, and dependency-audit checks pass locally. Untrusted-input and operator boundaries are not yet implemented. |
| Development/review gate — merge-blocking CI | FAIL | The workflow is present, but required GitHub protection/rulesets and an actual green PR run are absent. |
| Registry hygiene | FAIL | `[R]` and PR metadata are inconsistent for T001–T005. |

## Required before merge
1. Correct T001–T005 statuses to reflect the current no-PR/in-progress state; after opening the PR, record it and use `[R]` only for genuinely review-ready tasks.
2. Complete and document the T004/T005 merge-gate setup, obtain a green Node 22 PR run for both named jobs, and enforce those checks through GitHub protection/rulesets for `main` and `phase/**`.
